import { AboveTheFoldDirective } from './above-the-fold.directive';

describe('AboveTheFoldDirective', () => {
  it('should create an instance', () => {
    const directive = new AboveTheFoldDirective();
    expect(directive).toBeTruthy();
  });
});
