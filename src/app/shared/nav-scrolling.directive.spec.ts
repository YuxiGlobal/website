import { NavScrollingDirective } from './nav-scrolling.directive';

describe('NavScrollingDirective', () => {
  it('should create an instance', () => {
    const directive = new NavScrollingDirective();
    expect(directive).toBeTruthy();
  });
});
